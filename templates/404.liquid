<div class="page-width page-empty text-center">
    {% section 'banner-img-404' %}
  <hr>
    <h1>{{ 'general.404.title' | t }}</h1>
  <p>
    <a href="{{ routes.root_url }}" class="btn">{{ 'general.404.404_link' | t }}</a>
  </p>
</div>
